import type { ToasterProps } from "vue-sonner";

export const sonnerConfig: ToasterProps = {
    position: "top-right",
    richColors: true,
    closeButton: true,
    duration: 3000,
    expand: false,
};